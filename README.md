# Lista de Tarefas (To-Do List)

Este é um projeto simples de uma Lista de Tarefas (To-Do List) construído com HTML, CSS e JavaScript puro. Ele permite aos usuários adicionar tarefas e visualizá-las em uma lista.

## 🛠️ Tecnologias Utilizadas

* **HTML5:** Estrutura do documento.
* **CSS3:** Estilização e layout (responsivo e com tema de cores vibrantes).
* **JavaScript (Puro):** Funcionalidade para adicionar tarefas e manipulação do DOM.

## ✨ Funcionalidades

1.  **Adicionar Tarefa:** O usuário pode digitar um texto no campo de entrada e clicar no botão "Adicionar Tarefa" para incluir o item na lista.
2.  **Validação de Input:** Uma mensagem de erro é exibida se o usuário tentar adicionar uma tarefa com o campo de texto vazio.
3.  **Feedback de Sucesso:** Uma mensagem é exibida para confirmar que a tarefa foi adicionada com sucesso.

## 📁 Estrutura do Projeto

O projeto é composto pelos seguintes arquivos:

* `index.html`: Contém a estrutura da página (formulário de input, botão e lista de tarefas).
* `style.css`: Responsável pela aparência visual da aplicação.
* `script.js`: Implementa a lógica de adicionar tarefas ao DOM.
* `README.md`: Este arquivo.

## 🚀 Como Executar

Para executar este projeto localmente, siga os passos abaixo:

1.  **Clone ou Baixe o Repositório:** Obtenha os arquivos do projeto.
2.  **Abra o `index.html`:** Simplesmente abra o arquivo `index.html` no seu navegador de preferência (clique duas vezes no arquivo ou use a opção "Abrir com").

A aplicação será carregada e estará pronta para uso.
